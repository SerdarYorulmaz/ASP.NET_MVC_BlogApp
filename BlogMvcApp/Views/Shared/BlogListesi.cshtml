@model IEnumerable<BlogMvcApp.Models.BlogModel>


@if (Model.Count() == 0)
{
    <div class="alert alert-warning">

        <h3>Kayıt Bulunamadi..</h3>

    </div>
}


else
{
    foreach (var blog in Model)
    {
        <div class="blog">
            <h3 class="page-header">
                @blog.Baslik
            </h3>

            <p>
                <span class="glyphicon glyphicon-time"></span>
                @blog.EklemeTarihi

            </p>
            <img class="img-responsive" src="~/pexels-photo-459225.jpeg" />
            <hr />

            <p>
                @blog.Aciklama
            </p>
            @Html.ActionLink("Daha Fazla...", "Details", "Blog", new { id = @blog.Id }, new { @class = "btn btn-primary" })

        </div>
    }
}
